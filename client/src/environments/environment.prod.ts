export const environment = {
  production: true,
  // Replace this with your deployed backend URL once you deploy it
  // (e.g. to Render or Railway). Must include /api at the end.
  apiBase: "https://YOUR-BACKEND-URL.onrender.com/api"
};
